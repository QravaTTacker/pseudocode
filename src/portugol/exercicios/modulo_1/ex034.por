/*
 * Programa que mostra a velocidade (m/s) de um automóvel conforme
 * o deslocamento (m) e o intervalo de tempo (s) gasto para concluir
 * o trajeto:
 *
 * v = v0 + ∆x∆t
 */

programa
{
	funcao inicio()
	{
		escreva("Hello, World!")
	}
}
