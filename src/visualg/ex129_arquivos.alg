algoritmo "Arquivos"


arquivo "text.txt"

var
   i, x, len : inteiro
inicio
            len <- 5
   escreva("Teste Aquivo")
   leia(len)
   para i de 0 ate 5 faca
      escreva(numpcarac(i) + "º valor: ")
      leia(x)
   fimpara

fimalgoritmo