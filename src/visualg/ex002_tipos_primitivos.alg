algoritmo "Tipos primitivos"

var
   num: inteiro
   op: real
   msg: caractere
   fato: logico

inicio

   num <- 5
   op <- pi/2
   msg <- "olha para si mesmo"
   fato <- (2 > 0)ou(2 < 0)

   escreval(num)
   escreval(op)
   escreval(msg)
   escreval(fato)

fimalgoritmo